import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Moviles_LuisJesus_Cristian',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
